name: langevin-statfem
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - fenics
  - pip
  - pip:
    - arviz
    - h5py
    - ipython
    - notebook
    - matplotlib
    - pyDOE
    - pyamg
    - pytest
    - scipy
    - scikit-sparse
    - tabulate
    - jax[cpu]
